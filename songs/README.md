# 同梱曲

ここに音源を置き、`index.json` に並べると、初回起動時に「初期曲」としてライブラリへ入る。

```json
[
  { "file": "my-song.mp3", "title": "曲名" }
]
```

**配布して良い音源だけを置くこと**(自作曲、CC0、再配布が許可されたフリー素材など)。
このリポジトリは公開されているので、ここに置いたファイルは誰でもダウンロードできる。

